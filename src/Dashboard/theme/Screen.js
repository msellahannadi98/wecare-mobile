import { preferences } from 'joi';
import { create } from 'react-native-pixel-perfect';

const designResolution = {
    width: 398,
    height: 844
}

export default prefectSize = create(designResolution);